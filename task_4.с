#include <stdio.h>
#include <stdlib.h>
int main()
{
    int a,b;
    scanf("%d", &a);
    scanf("%d", &b);
    if(b%a==0){
        printf("число a является делителем числа b");
    } else{
        printf("число a не является делителем числа b");
    }
}
